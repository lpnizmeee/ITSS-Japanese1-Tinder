import { Link } from "react-router-dom";
import { Loader, Nav } from "../components";

export const RegisterPage = () => {
    return (
        <div>
            <Loader />
            <Nav />
        </div>
    )
}