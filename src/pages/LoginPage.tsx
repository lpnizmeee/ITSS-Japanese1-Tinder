import { Link } from "react-router-dom";
import { Loader, Nav } from "../components";

export const LoginPage = () => {
    return (
        <div>
            <Loader />
            <Nav />

        </div>
    )
}